[defaults]
# Skip checking the host key verification and direct Ansible to the inventory file
host_key_checking = False
inventory = inventory

[ssh_connection]
ssh_args = -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null

